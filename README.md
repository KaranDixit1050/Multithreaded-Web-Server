# Multi-Threaded Web Server in Java

A simple multi-threaded web server built using Java socket programming.  
The server handles multiple client connections concurrently by creating a new thread for each incoming request.

---

## 📌 Overview

This project demonstrates the fundamentals of **network programming** and **multithreading** in Java.  
The web server listens for incoming client connections, processes HTTP requests, and sends responses while supporting multiple clients at the same time using threads.

---

## 🚀 Features

- Handles multiple client requests concurrently
- Uses Java `Socket` and `ServerSocket`
- Thread-per-client architecture
- Basic HTTP request parsing
- Simple and extensible server design

---

## 🛠️ Technologies Used

- Java
- Socket Programming
- Multithreading
- Basic HTTP Protocol
