# Multithreaded-Web-Server
A lightweight multi-threaded web server built using Java sockets that handles multiple client requests concurrently. Each incoming connection is processed in a separate thread, enabling efficient request handling and improved performance.
